[pytest]
testpaths = tests
addopts = -p pytester
strict = true
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
asyncio_default_test_loop_scope = function
markers =
    integration: needs real PostgreSQL/Redis/OPA from docker compose. Must FAIL (not skip) when services are down.
