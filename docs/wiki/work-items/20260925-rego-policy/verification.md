# 검증 기록 — 4. 정책 (Rego) (작업 D)

> 요약
> - 결론: ② REVISE(1차)→APPROVE(r2), ④ 사이클1 REVISE(1차)→APPROVE(r2)·사이클2 APPROVE, ⑦ 사이클1 REVISE(1차)→REVISE(r2)→APPROVE(r3)·사이클2 APPROVE.
> - 바뀐 것: ② r2에서 변이 m의 기대 FAIL 집합을 5개→8개(exact)로, junit xml을 로그 해시로 보호. ④ r2에서 테스트 46개를 완전 비교로 강화. ⑦ r3에서 K10 승인 뒤 decisions.md 반영 순서를 "마무리"에서 "③ 시작 전"으로 앞당김.
> - 다음에 알아야 할 것: 사이클2 ⑥은 "코드 수정 없음"이며 남은 문제(변이 n·r·u 초과 FAIL 등)는 전부 후속(F-D8 등)·하네스(H9–H14)로 넘겼다. AC0–AC9 전부 통과.

| 관문 | 검증자 | 차수 | 최종 판정 | 주요 지적 |
|---|---|---|---|---|
| ② 계획 검증 | reviewer | 2 | APPROVE | 변이 m 목록 불완전(5→8개), junit xml 해시 보호 밖 |
| ④ 구현 검증 (사이클1) | reviewer | 2 | APPROVE | 46개 테스트가 계획의 "완전 비교" 표기보다 약한 형태로 구현됨 |
| ⑤ 테스트 (사이클1) | test-verifier | 1 | APPROVE | 지적 없음 |
| ⑥ 개선사항 계획 (사이클1) | planner | 3(r1·r2·r3) | "코드 수정 필요"→사이클2 | mail.send 필수화 거부 테스트 0개, decisions.md 반영 순서 |
| ⑦ 개선 계획 검증 (사이클1) | reviewer | 3 | APPROVE(r3) | K10 승인 뒤 decisions.md 보강 시점이 규칙과 어긋남(2회 REVISE) |
| ④ 구현 검증 (사이클2) | reviewer | 1 | APPROVE | 지적 없음 |
| ⑤ 테스트 (사이클2) | test-verifier | 1 | APPROVE | 지적 없음(AC0–AC9) |
| ⑥ 개선사항 계획 (사이클2) | planner | 1 | "코드 수정 없음" | — |
| ⑦ 개선 계획 검증 (사이클2) | reviewer | 1 | APPROVE | 참고 2건(cp949 바이트 오타, README 문구), 판정 사유 아님 |

## ② 계획 검증

### 1차 — REVISE

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| P1 | 선행 조건 완료 | 통과 | `docs/plan.md:21-23` A·B `[x]`, `docker-compose.yml:33` opa 1.20.2, `.github/workflows/ci.yml:32-42` opa-test job |
| P2 | 범위 한 항목 | 통과 | plan.md 4번 체크박스 5개 = stage1-plan 4장 D행 하나, "하지 않는다" 10개 항목으로 경계 분명 |
| P3 | D1–D9 충돌 없음 | 통과 | D1(fail-closed)을 정책 안에서 한 겹 더 구현, D5 준수 |
| P4 | 설계 변경 → decisions.md 초안 | **실패(REVISE)** | 초안 상세 절의 "변이 m 목록"이 5개(C1·C2·C3·C6+E2)로 적혀 있으나 계획 기대값을 손으로 추적하면 실제 8개(C4·C5·G6 누락) |
| P5 | 실패 경로 테스트 계획 | 통과(해당 없음) | 정책 자체는 외부 호출 없음. 게이트웨이 쪽 실패 경로는 작업 I로 이관 |
| P6 | 관련 불변 원칙 명시 | 통과 | 원칙 1·2·4·5·7 각각 방법 명시, 3·6은 "해당 없음" 이유 있음 |
| P7 | 완료 조건이 테스트 가능 | 통과 | AC0–AC8 모두 명령 원문·종료 코드·기대 출력 있음 |
| P8 | 폴더 규칙 준수 | 통과 | `policies/`·`tests/`에 배치, `docs/*`는 메인 세션 몫으로 분리 |
| P9 | 사람이 할 일 분리 | 통과 | 비밀·볼륨 삭제·클라우드 분리 없음 |
| P10 | 권한 규칙 대상 명령 없음 | 통과 | ENV·COMBINED 명령을 settings ask·deny 조각과 대조 완료 |
| P11 | AC마다 증거 | 통과 | 로그·xml·MANIFEST. 단 junit xml의 무결성 문제는 지적 2 |

지적 사항:
1. **[정확성·원칙7]** 변이 m(`approval_result → "require_approval"`)의 실패 목록이 5개로 적혀 있으나 실제 8개(C4 `test_mail_one_external_among_internal_denied`, C5 `test_mail_lookalike_domain_is_external`, G6 `test_extra_top_level_field_ignored` ②case 누락). decisions.md D18 상세에 틀린 문장이 들어갈 뻔했다.
2. **[증거]** junit xml이 `evidence.py`의 MANIFEST 해시 보호 밖인데(로그는 `.log`만 해시화) 변이 b·s, 모든 green, AC3의 판정 근거로 쓰인다.
3. **[권고, 비차단]** `ac_forbidden_commands.py`의 `label.startswith("u")`가 넓어 `u1-`뿐 아니라 `u`로 시작하는 모든 라벨을 제외한다.
4. **[권고, 비차단]** `ac_scope.py`의 `p in human` 부분 문자열 대조가 `docs/`처럼 넓은 경로를 적으면 그 아래 전부를 HUMAN으로 인정한다.

→ 반영: `plan_tests.py MUTATIONS["m"]`을 8개+`exact=True`로, COMBINED·AC3 명령이 pytest 뒤 xml 해시·`exit_pytest=N`을 로그에 남기고 `_ev.verified_junit()`이 대조, `ac_forbidden_commands.py`는 `re.match(r"^u\d", label)`로 좁힘, `ac_scope.py`는 백틱으로 감싼 토큰의 정확 일치만 인정.

### 2차(r2) — APPROVE

1회차 지적 4건 전부 반영 확인. 변이 m 8개를 검증자가 직접 재추적해 일치 확인(A 23·B 9·D1–D4·E1·E3·F1·G1–G5는 PASS, C1–C6·E2·G6은 FAIL로 정확히 8개). `verified_junit()` 로직을 직접 읽고 "닫히는 방향의 분기만 있다"고 확인. 새 구멍 없음, 기준 완화 없음(전부 조건 추가·정확 집합·인정 범위 축소 방향). checks 해시를 `02-plan-review.r2.md` "checks 해시" 절에 재부착.

## 🧑 계획 승인

- 승인 일시: 2026-09-25 17:43 (+0900)
- 사용자 답 원문: "권장안대로 승인하고 B 문서도 지금 커밋해주세요"
- U0: D18 확정 + K1–K9 권장안 그대로 승인(K8 "알 수 없는 최상위 필드 무시" 관대한 쪽 결정 포함)
- K10(사이클2, 2026-09-25 19:36): D18 상세에 `mail.send` subject·body 필수(A안) — 사용자 답 원문 "A안으로 진행해주세요"

## ④ 구현 검증 (사이클 1)

### 1차 — REVISE

| # | 항목 | 결과 | 근거 (파일:줄) |
|---|---|---|---|
| A1 | 바뀐 파일 = 계획 | 통과 | `git status`: authz.rego·authz_test.rego·test_policy_static.py + 사람 변경 decisions.md(00-approval.md 기록) |
| A2 | 계획한 테스트 전부 작성 | **실패(부분)** | 53개 이름·입력·기대 result/reason은 계획과 일치하나 46개가 "필드 개별 비교" 형태(계획이 정의하지 않은 3번째 형태) |
| A3 | "하지 않는다" 준수 | 통과 | ci.yml·decisions.md·plan.md 등 보호 파일 해시 불변 |
| B1 | 기본 결과 deny, 허용 판정 오류 없음 | 통과 | `default decision` 거부 유지, else 체인 순서, `allow_ok` 양성 조건만 |
| B2 | 외부 호출 타임아웃 | 해당 없음 | 정책에 외부 호출 없음 |
| B3 | 자격 증명 비노출 | 통과 | 입력 계약에 토큰·키 없음, `opa.runtime` 금지(S2) |
| B4 | 확인·차감 분리 없음 | 해당 없음 | 건당 한도만 비교, 예약·차감은 이 작업 밖 |
| B5 | 판단+outbox 트랜잭션 | 해당 없음 | 게이트웨이(I) 범위 |
| B6 | 캐시 버전 확인 | 해당 없음 | 캐시 없음 |
| B7 | 테스트가 결과·이유 assert | 통과(형태는 지적1) | A–D·G 전부 result·reason 확인 |
| C1 | 비동기·경쟁 조건 | 해당 없음 | Rego·정적 pytest뿐 |
| C2 | 하드코딩된 비밀 없음 | 통과 | 확인함 |

지적 사항:
1. **[계획 일치·원칙7]** `policies/authz_test.rego` 46개(A·B·C1–C5·D·G2–G6)가 계획이 정의한 "완전 비교" 또는 "result만 비교" 두 형태가 아니라 `d.result == X; d.reason == Y` 형태(3번째 형태)로 구현됨. 이 형태는 출력에 키가 더 있어도 통과한다. 변이 u(허용 분기 키 추가)가 이 문제를 드러냈고 ③은 그 하나만(G1) 완전 비교로 고쳤다.
2. **[계획 일치·계약 기록]** `mail.send`의 `subject`·`body` 문자열 필수화(`authz.rego:103-104`)가 계획 밖 추가인데 03-build-notes에 기록되지 않음.
3. **[증거 서술]** "로그 94개 전부 일치" → 실제 93개(헤더 제외).

→ 반영: 46개를 `d == {"result": X, "reason": Y}` 완전 비교로 강화(이름·입력·기대값 변경 없음). 지적 2는 03.r2에 기록. 지적 3은 수치 정정.

### 2차(r2) — APPROVE

diff로 51쌍 검증: 삭제 줄은 전부 `d.result==`/`d.reason==` 형태, 추가 줄은 전부 완전 비교 형태이고 값 불일치(mismatched)=0. 정책은 1회차와 동일 해시(`e2acae27…`). 변이 21종 재실행에서 m(exact 8)·p(exact 1)·u(부분집합, 이제 7개 FAIL로 늘어남) 모두 사전 추적과 정확히 일치. 탐침 재실행(거부 분기에 키 추가)이 1/53→36/53 FAIL로 늘어 지적1의 목적이 실제로 달성됨을 확인.

## ⑤ 테스트 (사이클 1)

APPROVE. AC0–AC8 전부 통과. 상세는 [testing.md](testing.md).

## ⑥ 개선사항 계획 (사이클 1)

| 차수 | 판정 | 요지 |
|---|---|---|
| r1 | 코드 수정 필요 → 사이클2 | (a) 46개 완전 비교 강화는 이미 끝났으나 근본 원인(거부·승인 분기에 표기 강제 변이 없음)을 변이 x·y로 영구 증거화. (b) `mail.send` 필수화 규칙에 거부 테스트 0개 → 테스트 1개+변이 v·w 추가, D18 K10 사용자 확인 필요. (c) 03의 "94개" 오기 지적 |
| r2 | 동일 | ⑦ 1차 지적(decisions.md 반영 순서) 반영, 나머지 변경 없음 |
| r3 | 동일 | ⑦ 2차 지적(`build-c2-env`의 grep 패턴이 사이클1 머리 줄에도 걸림) 반영, 패턴을 `K10(A`로 좁힘 |

## ⑦ 개선 계획 검증 (사이클 1)

### 1차 — REVISE

기준 완화 탐지: 없음(assert 약화·테스트 삭제·AC 축소·원칙 예외 모두 없음). 체크리스트 I1–I8 대부분 통과, 변이 x=37·y=7 독립 추적 결과 일치.

지적 사항: **K10 승인 뒤 `docs/decisions.md` D18 보강을 "마무리"로 미룬 것이 `work-item/SKILL.md:67`("승인받으면 메인 세션이 decisions.md에 추가한 뒤 ③으로")과 CLAUDE.md 작업 방식(설계 결정은 코드보다 먼저)에 어긋난다.** ⑥이 미룬 근거 2가지(③–⑤ 동안 추적 파일 금지, AC6 해시 보호)는 모두 성립하지 않는다 — 전자는 ③ 시작 **전** 수정을 막지 않고, 후자는 `00-approval.md` 표에 백틱 전체 경로를 적으면 HUMAN으로 처리되는 기존 장치로 해결된다.

### 2차(r2) — REVISE

r1 지적을 반영해 순서를 "K10 승인 → decisions.md 보강 + 00-approval.md 기록(같은 시각) → 사이클2 ③"으로 교정했으나, `build-c2-env`의 `--- approval c2` grep 패턴 `"docs/decisions.md"`가 사이클1 머리 줄(00-approval.md 6행)에 이미 매치돼 표 행이 없어도 멈춤 조건이 발동하지 못하는 문제를 새로 지적.

### 3차(r3) — APPROVE

grep 패턴을 `K10(A`(표 행 템플릿 "K10(A안)"에만 매치, 머리 줄 템플릿 "K10 (사이클2)"에는 불일치)로 좁혀 반영. AC6 HUMAN 기대를 "정확히 1줄"로 명시. 검증자가 실제 `00-approval.md`에서 `K10(A` 0건임을 확인. 나머지(판정, 변이 v·w·x·y, checks_c2)는 이미 타당하다고 판단해 변경 없음.

## ④ 구현 검증 (사이클 2)

### 1차 — APPROVE

- 사이클 2 변경이 새 테스트 20줄 추가뿐임을 해시 대조로 확인(`ceb6e129…`가 사이클1 최종 파일과 일치, 나머지 461줄 동일).
- 정책 파일이 사이클 2 내내 기준선(`e2acae27…`)으로 유지·복원됨(25개 변이 red 사이 매번).
- v·w·x·y FAIL 집합이 06-improvement-plan.r3.md 표와 정확히 일치(v=w={새 테스트}, x=37개, y=7개 — 이름까지 대조).
- builder가 요청한 3가지 판정: (1) 변이 n·r·u 초과 FAIL 통과 처리 — 타당(사이클1과 완전히 같은 이름 집합, 안전선 유지, `plan_tests.py`에 exact 미지정이 원인이나 결과를 본 뒤 exact로 고정하면 원칙7 위반이라 ③이 고치지 않은 것이 맞음). (2) 변이 s 되돌림 중 개행 삭제를 `printf`로 복구한 것 — 문제없음(최종 해시가 기준선과 같음, 자기 발견·자기 수정으로 은폐 아님). (3) AC9 미실행 — 맞다(③ 자기 점검 범위 밖, ⑤가 실행).
- 탐침: `subject`/`body` 검사를 타입 검사에서 존재 검사로 약화해도 새 테스트가 잡음을 확인(FAIL 1/54).

## ⑤ 테스트 (사이클 2)

APPROVE. AC0–AC9 전부 통과. 상세는 [testing.md](testing.md).

## ⑥ 개선사항 계획 (사이클 2)

판정: **"코드 수정 없음."** ④·⑤ 모두 APPROVE, AC0–AC9 전부 `0 bad`, `--verify` 264개 전부 일치. 남은 6가지 문제(변이 n·r·u 초과 FAIL, 종료 코드 열 혼동, 개행 삭제, Docker 꺼짐 무효 로그, MSYS 경로 변환 무효 로그, cp949 인코딩 깨짐)는 전부 계획 규칙·기록 방식·도구 문제이고 제품 코드·테스트를 바꿔 고칠 대상이 아니어서 후속 항목(F-D8·F-D9)과 하네스 개선안(H9–H14)으로 넘겼다. 특히 n·r·u의 관측값을 지금 exact로 고정하면 원칙 7 위반이라고 명시했다.

## ⑦ 개선 계획 검증 (사이클 2)

### 1차 — APPROVE

기준 완화 탐지: 없음. 체크리스트 I1–I8 전부 통과. "코드 수정 없음" 판정이 타당함을 확인 — 후속으로 미룬 것 중 원칙 위반이나 실패 AC는 없고(초과 FAIL 10개 전부 거부 방향, 안전선 유지), 지금 고치는 두 대안(exact 고정=원칙7 위반, 정책 재수정=사이클 3회로 한도 초과) 모두 나쁘므로 마무리로 가는 것이 맞다고 판단.

참고(조치 권장, 판정 사유 아님):
1. README 초안 문구 2곳이 다소 느슨함("push·PR마다" → "main 푸시·PR마다"가 정확).
2. **06.c2 문서의 cp949 바이트 표기 오타**: "기록됨"의 cp949는 `B1 E2 B7 CF B5 CA`이다(`C6` 아님). 결론(cp949→UTF-8 replace)은 맞다.
3. 마무리 10번 정리 목록에 `checks_c2/__pycache__/` 누락.

## checks 해시 기록 (참고)

- ② r2: `plan_tests.py 42fa7ad6…`(이후 사이클 내내 불변)
- ⑦ 사이클1(r1·r2·r3), 사이클2: `checks_c2/c2_evidence.py 46f07fb4…`(불변, `.c2.md` 파일명은 이름순 정렬에서 `.r3.md`보다 앞이라 AC9 기준은 계속 `.r3.md`)
