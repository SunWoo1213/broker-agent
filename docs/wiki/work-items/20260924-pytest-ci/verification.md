# 검증 기록 — 0. 개발 환경 — pytest · CI

> 요약
> - 결론: ② APPROVE(r3 대상, 1회 판정) · ④ 1차 REVISE → 2차 APPROVE · ⑦ APPROVE(코드 수정 없음 판정 확인).
> - 바뀐 것: ④ 1차 REVISE 지적 2건(T11 권한 확장 미검출, T8 job별 timeout 미검사) 때문에 `tests/test_ci_workflow.py`가 강화되고 변이 f·g·h가 추가됐다.
> - 다음에 알아야 할 것: ④ r2 지적 참고 1("`ac8_workflow_structure.py`도 같은 한계")은 25행을 놓친 부정확한 서술이었고 ⑦이 정정했다(아래 ⑦ 절).

## 요약

| 관문 | 검증자 | 차수 | 최종 판정 | 주요 지적 |
|---|---|---|---|---|
| ② 계획 검증 | reviewer (fable) | r3 (1회 판정) | APPROVE | REVISE 사유 없음. 참고 지적 5건(권고·기록용) |
| ④ 구현 검증 | reviewer (fable) | 2차 | APPROVE | 1차 REVISE 2건 → r2에서 해소 |
| ⑦ 개선 계획 검증 | reviewer (fable) | 1차 | APPROVE | 06 도입부 문장 오류 1건, ④ r2 자신의 서술 오류 1건 정정 |

## ② 계획 검증

계획은 r1 → r2 → r3로 세 차례 갱신됐지만, **검증(②)이 실제로 실행된 것은 r3 한 번뿐**이다. r1→r2는 사용자 지시("테스트를 했다 하고 끝내는 것이 아니라 실제로 수행한 흔적과 그 증거가 있어야 한다")에 따라 planner가 스스로 고친 것이고, r2→r3는 evidence.py 권한 필터 구멍을 planner가 스스로 반영한 것이다. `02-plan-review.md`는 "검증 대상: `01-plan.r3.md` (r1·r2는 변경 이력 확인용으로만 대조)"라고 명시한다.

### r3 — APPROVE (1차이자 최종)

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| P1 | 선행 조건 완료 | 통과 | `docs/plan.md:21-22` `[x]`, 커밋 `780b009`. `.venv`에 pytest 9.0.3·pytest-asyncio 1.4.0 설치 확인. pytest 소스 줄 4곳·pytest-asyncio ini 3개·훅 등 계획이 인용한 근거가 실제 파일과 일치 |
| P2 | 범위가 항목 하나, "하지 않는다" 있음 | 통과 | "한다" 4개, "하지 않는다" 11개 명시 |
| P3 | D1–D9와 충돌 없음 | 통과 | 브로커 코드 경로 없음. D1(fail-closed)을 테스트 설정·CI 자신에 적용 |
| P4 | 설계 변경 시 decisions.md 초안 | 통과 | "설계 변경 여부: 없음". stage1-plan과 다른 두 곳(Windows 루프·CI 서비스 컨테이너)은 "순서 조정"으로 명시하고 오케스트레이터·사용자 결정으로 넘김 |
| P5 | 외부 의존성 실패 경로 테스트 | 통과(해당 없음 + 대체) | 브로커 외부 호출 코드 없음. 대신 "설정 자체가 실패를 삼키지 않는" 경로(T1–T5, T8)를 먼저 씀. 네트워크 실패는 `ac11_action_shas.py`가 BAD로 처리(추측 없음) |
| P6 | 관련 불변 원칙 명시 | 통과 | 원칙 1·2·7 각각 항목별 방법 있음. 3–6 "무관" 명시 |
| P7 | 완료 조건이 테스트 가능 | 통과 | AC0–AC14 표 전부 "명령 원문 + 종료 코드 + 기대 출력" |
| P8 | 폴더 규칙 준수 | 통과 | `tests/`, 루트 `pytest.ini`, `.github/workflows/ci.yml` 표준 위치. `checks/`·`evidence/`는 `.claude/runs/`(`.gitignore`로 제외) |
| P9 | 사람이 할 일 분리 | 통과 | 비밀 없음, 삭제 단계 없음, 클라우드 없음, 푸시는 U1로 사람 확인 |
| — (P10 상당) | AC·절차에 deny·ask 명령 없음 | 통과 | `evidence.blocked_by()` 실제 호출로 확인(아래) |
| — (P11 상당) | AC마다 증거가 명시됨 | 통과 | AC 표 "증거로 남는 것" 열 전부 채워짐 |

**P11 보강 — `evidence.blocked_by()` 실제 호출 결과**: 계획의 evidence.py 명령 36개를 실제 함수에 넣어 확인(실행은 안 함). 36개 전부 `None`(통과). 대조군 4개(`git push -u origin main`, `curl -s "..."`, `git remote -v`, `docker run --rm ...`)는 전부 126 거부 대상으로 판정됨 → 계획의 "U1·U2는 직접 실행 → raw 파일 → cat" 분리가 필요하고 충분함을 확인.

**오케스트레이터가 물은 4가지 중 하나 — planner가 `checks/` 판정 스크립트를 쓴 것**: `planner.md`가 "쓰기 권한은 `.claude/runs/` 아래뿐"이라 규칙 위반은 아니라고 판정했으나, "코드는 쓰지 않는다"는 문장이 이 관행을 명시적으로 허용하지 않는다는 점을 지적(지적 3, 하네스 기록 권고). 8개 스크립트 전부를 읽어 기준 완화·우회가 없음을 확인.

지적 사항 → 반영 내용:

1. [권고] job 수준 `permissions:`는 자동 판정 어디에도 안 걸림 → **④ 구현 검증에서 눈으로 확인**하도록 넘김 (④가 실제로 확인, B3 항목)
2. [기록 정합성] `checks/_ev.py`·`ac9_workflow_commands.py` 독스트링이 r2를 가리킴 → r2·r3 골격이 같으므로 고치지 말고 그대로 둠(해시 고정 규칙 우선)
3. [하네스] planner의 "코드는 쓰지 않는다" 문장과 채점 스크립트 관행 불일치 → 오케스트레이터가 `harness-notes.md`에 기록하고 `planner.md` 수정은 사용자에게 제안하도록 권고. **반영됨**: `agents/planner.md:23-26`에 예외 절 추가(⑥ "반영 완료" 목록)
4. [흐름] `plan.md` 체크가 U2 뒤로 밀려 두 번째 커밋(U3)이 생김 → 계획 68줄이 이미 "③이 아니라 오케스트레이터가"로 명시. 🧑 계획 승인 때 사용자에게 그대로 보여줄 것으로 처리
5. [범위 밖] 저장소 루트의 untracked `.claudesettings.local.json` → 이번 작업 필터·판정에 영향 없음. 사용자가 정리할 것

## 🧑 계획 승인

- 승인 일시: 2026-09-24 22:30 (+0900)
- 사용자 답 원문: "네 넣어주시고 구현 시작해주세요" (질문: planner.md에 채점 스크립트 허용 문장 추가 여부 + 구현 시작 여부)
- 작업 중 사람이 한 추가 변경: 2026-09-24 22:47 (+0900), 오케스트레이터가 사용자 지시로 하네스 템플릿(`docs/wiki/_templates/` 4개) 및 `dev-wiki/SKILL.md`·`harness-notes.md`를 변경. 이 작업(③)의 행동이 아님. 사용자 답 원문: "네 그렇게 진행해주세요"

## ④ 구현 검증

### 1차 — REVISE

| # | 항목 | 결과 | 근거 (파일:줄) |
|---|---|---|---|
| A1 | 바뀐 파일 = 계획 | 통과 | `git status` 새 줄 5개가 계획 "바꿀 파일"과 정확히 일치 |
| A2 | 계획한 테스트 전부 작성 | **실패(부분)** | T1–T11 이름 11개 모두 존재하나, **T11**(`test_ci_workflow.py:74-78`)이 계획 "최상위 `permissions:` 아래가 `contents: read` **하나다**"를 검사하지 않고 다음 줄 하나만 봄. **T8**(`:43-48`)이 계획 "**모든** job에 `timeout-minutes:`"를 검사하지 않고 줄 개수(2)만 봄 |
| A3 | "하지 않는다" 준수 | 통과 | Selector 루프 없음, CI에 DB·Redis 없음, `pythonpath`·coverage·ruff·`cache: pip` 없음, 보호 파일 12개 해시 불변, `git add`·commit·push 없음 |
| B1 | 기본 결과가 deny (원칙 1) | 통과(설정에 적용) | `pytest.ini` strict·asyncio_mode=strict, `ci.yml` pipefail·`if: always()` 유일·0건 방지 grep·`if-no-files-found: error`·timeout 10 |
| B2 | 외부 호출 타임아웃 | 통과 | 두 job 모두 `timeout-minutes: 10`, ls-remote `timeout=60` |
| B3 | 자격 증명 비노출 (원칙 2) | 통과 | `secrets.` 없음, `persist-credentials: false` 2곳, 최상위 `permissions: contents: read` 하나뿐이고 **job 수준 permissions 없음을 눈으로 확인**(② 지적 1 대응 완료) |
| B7 | 테스트가 결과·이유·도구 미호출 확인 | 통과 | T1–T5·T9의 assert가 결과+메시지를 모두 확인 |
| C1 | 비동기·경쟁 조건 | 통과 | 비동기 코드는 T7뿐, `tmp_path` 격리 |
| C2 | 하드코딩된 비밀 없음 | 통과 | 새 파일 4개 + README diff에 없음 |
| D1 | 03 자기 점검 증거 일치 | 통과 | 03 표 21행 ↔ MANIFEST 21행 라벨·종료 코드 전부 일치 |
| D2 | 실패 먼저 확인 증거 | 통과 | `05-build-red-initial.log` exit 1. 변이 a–e red xml에서 실패한 테스트 = 계획 지정 테스트와 정확히 일치 |
| D3 | `--verify` 전부 일치 | 통과 | `22-review-verify.log` 21개 전부 `일치` |

지적 사항 → 반영 내용:

1. [원칙 2·필수] T11이 `permissions:` 확장을 놓침. 탐침: `pull-requests: write` 추가해도 T11 통과해버림 → **r2에서 반영**: `permissions:` 줄보다 깊게 들여쓰인 연속 줄 전체를 모아 `== ["contents: read"]`로 비교(`test_ci_workflow.py:94-107`). 변이 f 증거(`26`–`27` 로그)
2. [원칙 1·필수] T8이 job별 timeout 누락을 놓침. 탐침: timeout 없는 job 추가해도 T8 통과해버림 → **r2에서 반영**: `_job_blocks` 헬퍼로 job마다 timeout 존재를 확인(`:32-42, 60-65`). 변이 g 증거(`28`–`29` 로그)
3. [참고·범위 밖] 템플릿 4파일이 22:47:31에 바뀜(③ 완료 이후, ④ 진행 중) → `00-approval.md`에 기록되어 있어야 ⑤가 대조 가능. 당시 표가 비어 있었다는 지적 → r2에서 `00-approval.md`에 기록 완료
4. [참고, 선택] README "단위만 (기본값, 통합 제외)" 문구 모순 → **r2에서 반영**: "단위만 (통합 제외)"로 정정
5. [참고, 선택] T10(OPA 명령 리터럴 비교)의 red 증거 없음 → **r2에서 반영**: 변이 h 추가(0건 방지 grep 약화 → T10 실패, `30`–`31` 로그)

### 2차(r2) — APPROVE

| # | 항목 | 결과 | 근거 (파일:줄) |
|---|---|---|---|
| A1 | 바뀐 파일이 지적 범위 안 | 통과 | `git status` 1차와 동일 5줄. `ci.yml` 해시 `ef15003c…`, `pytest.ini` 해시 `706f7fed…` = 1차 baseline과 동일 |
| A2 | 지적 1 해소 (T11 "하나") | 통과 | 탐침 5종(두 번째 권한 추가·빈 줄 뒤 추가·`contents: write`·job 수준 permissions·원본) 전부 기대대로: 1차 탐침에서 통과했던 것이 이제 실패 |
| A2 | 지적 2 해소 (T8 "모든") | 통과 | 탐침 5종(끝에 추가·중간 삽입·기존 job timeout 제거·job 이름 변경·원본) 전부 기대대로 |
| A3 | 다른 검사가 약해지지 않음 | 통과 | `continue-on-error`·`\|\| true`류·`set +e`·`if:` 유일성·`shell: bash` 그대로. T9·T10 변경 없음. `_job_blocks`는 `jobs:` 뒤만 보므로 `defaults.run`을 job으로 오인하지 않음 |
| B7 | 결과+이유 assert | 통과 | T11·T8 실패 메시지에 원인(추가된 줄, job 이름)이 찍힘 |
| D1 | 03.r2 자기 점검 증거 일치 | 통과 | 03.r2 표 8행(25–32) ↔ MANIFEST 25–32 라벨·종료 코드 일치 |
| D2 | 변이 f·g·h가 의도한 테스트·이유로 실패 | 통과 | f/g/h 각각 red에서 정확히 1건 실패, green에서 baseline 해시로 완전 복원 |
| D3 | `--verify` 전부 일치 | 통과 | `33-review-r2-verify.log` 01–32 전부 `일치` |

지적 사항 → 반영 내용(참고·후속용, REVISE 아님):

1. [참고 후속 후보] `JOB_HEADER_RE = ^  ([a-zA-Z][\w-]*):`가 `_`로 시작하는 job id를 놓침(GitHub 문서: 문자 또는 `_`로 시작 가능). 실제 시나리오(평범한 job 추가·timeout 누락·권한 확대)는 전부 막혔으므로 3차수를 열 사유는 아님으로 판단 → **⑥에서 F1으로 이관**(수정 없이 후속). ※ 이 지적의 "`ac8_workflow_structure.py`도 같은 한계" 서술은 25행(job 집합 전체 비교)을 놓친 부정확한 판단이었고, ⑦에서 정정됨(아래 ⑦ 절 참고)
2. [⑤에게] 변이 f·g·h는 계획 변이 표(a–e) 밖 추가 증거라 `ac6_build_evidence.py`가 채점하지 않음 → 판정은 이 리포트 D2 행으로 갈음
3. [⑤에게] 템플릿 4파일은 `00-approval.md`에 기록됨. `ac12-scope`의 BAD 4줄은 그 행과 대조해 리포트에 적을 것

## ⑦ 개선 계획 검증

### 1차 — APPROVE (판정: 코드 수정 없음 확인)

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| — | 기준 완화 탐지 | 없음 | assert 약화·테스트 삭제/skip/xfail·타임아웃만 늘리기·AC 변경·채점 기준 수정·불변 원칙 예외 추가 전부 "없음". `checks/*.py` 8개 sha256 = 기준선과 동일 |
| I1 | 지적 사항 누락 없음 | 통과 | 04 r1 지적 1–5, 04 r2 참고 1–3, 05 AC12·변이 f/g/h, 03 특히 4, 02 지적 2–5까지 06 표·"사용자 할 일"에 모두 있음 |
| I2 | 근본 원인이 증상 반복이 아님 | 통과 | "변이 표가 검증 문장(수량·전칭)을 덮지 않음"과 "checks·tests 검사 강도 불일치"라는 구조적 원인으로 지적 1·2·5를 한데 묶음. 계획·ac8 코드로 실제 확인됨 |
| I3 | 수정안이 원인을 직접 고침 | 통과 | 원인 (1)→H1·H7, 원인 (2)→H2·H3, 지적 3→H4·H5, 03 특히 4→H6 |
| I4 | 새 원칙 위반 없음 | 통과 | F1·H1–H7 전부 검사를 더하는 방향. U2 실패 분기에 금지 해결책(버전 고정 해제·continue-on-error·if 추가·assert 완화 금지) 명시 |
| I5 | "수정 없음" 판정의 정합성 | 통과 | `04-code-review.r2.md`·`05-test-report.md` 둘 다 `VERDICT: APPROVE` |
| I6 | 하네스 개선안이 구체적 | 통과 | H1–H7 전부 삽입 위치(파일·절)와 인용문 형태로 적힘. "반영 완료" 3건도 실제로 존재 확인 |
| I7 | 개선 사이클 ≤ 2 | 통과 | 사이클 1. ④ r2는 사이클 안의 REVISE 왕복 |
| I8 | 06 인용 결과 = 05 증거, `--verify` 전부 일치 | 통과(문구 정정 1건) | `60-review-i-verify.log` 01–59 전부 `일치`. 06 도입부 "AC0–AC14 중 14개는 exit 0"이 부정확(AC5는 계획 기대값대로 exit 5) → 지적 1로 정정 |

**오케스트레이터가 물은 것 — "코드 수정 없음 + F1 이관"이 기준 완화인가**: 아니다. 기존 assert·AC·변이·원칙을 건드리지 않고, 남은 결함을 후속 항목으로 이름 붙여 넘기며 구체 수정 내용과 트리거 조건("U2로 사이클 2가 열리면 같은 사이클에")을 적었기 때문. 다만 F1은 사용자에게 plan.md 항목으로 제안되는 것이라, 사용자가 거절하면 결함이 기록만 남는다(숨김은 아님).

**⑦ 자신의 정정**: ④ r2 참고 1의 "`ac8_workflow_structure.py:31`도 이름이 고정된 두 job만 보므로 같은 한계"라는 서술은 25행(`sorted(jobs) == ["opa-test", "pytest"]`로 job 집합 전체 비교)을 놓친 부정확한 판단이었다. 06이 이를 정정했고, ⑦은 "06이 맞다"고 확인했다. 남는 구멍은 "CI 안의 T8만, `_`로 시작하는 job에 timeout까지 빠뜨렸을 때"이며 GitHub 기본 job 상한(360분)이 있어 원칙 1 위반(실패를 통과로 바꾸는 경로)이 아니라 대기 상한 문제로 분류.

**⑦ 통과 뒤 후속 결정 (run 문서 밖, `.claude/harness-notes.md` 76–91행)**: 06이 제안한 H1–H7 중 사용자가 "네 추천대로 진행해주세요"로 결정해 H1·H2·H4·H5·H7을 실제로 반영했다(H3는 H1에 합침, H6은 보류). I6 판정 당시("삽입 위치가 실존"만 확인)와 달리, 그 뒤 파일에 실제 문장이 들어갔다. 상세: [index.md](index.md) 7절.

지적 사항 → 반영 내용:

1. [기록 정정] 06 도입부 "AC0–AC14 중 14개는 exit 0"이 부정확(AC5는 exit 5) → 오케스트레이터가 06 끝에 정오표를 추가해 반영. **본 위키 문서의 수치는 06이 아니라 05 표에서 직접 옮겼다**
2. [정정] ④ r2 자신의 "ac8도 같은 한계" 서술 오류 → 이 문서(verification.md) ④ r2 절에 정정 사실을 함께 적음
3. [⑤·위키에게] `evidence/60-review-i-verify.log`는 ⑦이 남긴 로그. 위키로 `evidence/`를 복사한 뒤 `--verify`를 다시 돌리면 60개가 나와야 함 → 본 작업에서 실행, [testing.md](testing.md) "증거 보존" 절 참고
